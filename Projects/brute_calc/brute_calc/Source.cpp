#include <iostream>

using namespace std;

int main()
{
	char a;
	cout << "write length of password or write 0 if you want to write not only one lenth of password" << endl;
	cin >> a;
	if (a == '0'){
	
	}
	else{
		cout << "how much symbols do you have to find it with brute force?" << endl;
		cout << "and how much counters password bruted in 1 second?" << endl;
	}



	return 0;
}